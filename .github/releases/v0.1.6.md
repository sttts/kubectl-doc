## What's Changed

### New Features

- **JSON Schema output** (`-o jsonschema`): Export a plain JSON Schema YAML document for a resource or CRD. Useful for tooling integration, validation pipelines, and editor autocomplete.

### Improvements

- Terminal colour styles are now shared across renderers, giving consistent colouring in YAML and TUI output.
