## What's Changed

### Performance

- Web browser mode filtering is significantly faster: field visibility is now index-driven, reducing DOM work on each keypress.

### Improvements

- Web browser mode: Escape key correctly clears the active filter.
- Web browser mode: Filter status chip is now displayed above the schema content for better visibility.
- TUI: Filter status chip is colour-coded to reflect the active filter state.
